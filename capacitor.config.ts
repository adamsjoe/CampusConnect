import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.CampusConnect.app',
  appName: 'CampusConnect',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
